server:
  port: 8082

spring:
  application:
    name: catalog-service
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true
